<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">PWD Manager</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/">Home</a>
          </li>
            <div id="unauthenticated">
              <li class="nav-item"  style="float: left;">
                <a class="nav-link" href="/login">Login</a>
              </li>
              <li class="nav-item" style="float: right;">
                <a class="nav-link" href="/signup">Sign up</a>
              </li>
            </div>
            <div id="authenticated">
              <li class="nav-item" style="float: left;">
                <a class="nav-link active" aria-current="page" href="/pwds">pwds</a>
              </li>
              <li class="nav-item" style="float: right;">
                <button class="btn btn-danger nav-link logout" onclick="return logout()" style="color: white;">Logout</button>
              </li>
            </div>   
      </div>
    </div>
</nav>
<script>
  var authenticated = document.getElementById("authenticated");
  var unauthenticated = document.getElementById("unauthenticated");

  function getCookie(name) {
    var dc = document.cookie;
    var prefix = name + "=";
    var begin = dc.indexOf("; " + prefix);
    if (begin == -1) {
        begin = dc.indexOf(prefix);
        if (begin != 0) return null;
    }
    else
    {
        begin += 2;
        var end = document.cookie.indexOf(";", begin);
        if (end == -1) {
        end = dc.length;
        }
    }
    // because unescape has been deprecated, replaced with decodeURI
    //return unescape(dc.substring(begin + prefix.length, end));
    return decodeURI(dc.substring(begin + prefix.length, end));
}
var myCookie = getCookie("jwt");
if (myCookie == null) {
  unauthenticated.style.display = "inline";
  authenticated.style.display = "none";
}
else {
  unauthenticated.style.display = "none";
  authenticated.style.display = "block";
}
</script>